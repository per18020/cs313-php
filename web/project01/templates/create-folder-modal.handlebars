<div>
    <div class="box">
        <div class="field">
            <label class="label">Collection Name</label>
            <div class="field has-addons">
                <div class="control is-expanded">
                    <input id="{{this.input_id}}" class="input" type="text" placeholder="Untitled Collection" maxlength="30">
                </div>
                <div class="control">
                    <div id="{{this.button_id}}" class="button is-info">Create Collection</div>
                </div>
            </div>
            <p class="help">
                Max length of 30 characters
            </p>
        </div>
    </div>
</div>